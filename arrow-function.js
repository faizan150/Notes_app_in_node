const add = function (x,y)
{
    return x+y
}
console.log('The Addition of 2 numbers is '+add(45,40));

const subtract = (x,y) => x-y

console.log('The Subtraction of 2 numbers is ' + subtract(49,10));
